{
  "name": "oldbaynewfoundlands",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "node server.js -p $PORT",
    "build": "next build",
    "start": "NODE_ENV=production node server.js -p $PORT",
    "export": "next export",
    "deploy": "rimraf node_modules/.cache && rimraf out && next build && next export && nodetouch out/.nojekyll && git add out/ && git commit -m \"Deploy to master from source\" && git subtree push --prefix out origin Master"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cindyflowers/oldbaynewfoundlands"
  },
  "devDependencies": {
    "babel-plugin-transform-define": "^1.3.2",
    "gh-pages": "^2.1.1",
    "rimraf": "^3.0.0",
    "touch": "^3.1.0"
  },
  "dependencies": {
    "@zeit/next-css": "^1.0.1",
    "@zeit/next-sass": "^1.0.1",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "next": "9.1.5",
    "next-compose-plugins": "^2.2.0",
    "next-fonts": "^0.19.0",
    "next-optimized-images": "^2.5.4",
    "path": "^0.12.7",
    "react": "16.12.0",
    "react-dom": "16.12.0"
  }
}
